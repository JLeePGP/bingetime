# BingeTime.tv — backend dependencies (Python 3.14, all-Python stack)
fastapi>=0.115
uvicorn[standard]>=0.30
jinja2>=3.1
python-multipart>=0.0.9          # form parsing (calculator/planner/story POSTs)
pydantic>=2.9
pydantic-settings>=2.4           # env-driven config

# Data layer: SQLAlchemy 2.0 + Alembic + psycopg 3 (best 3.14 support)
SQLAlchemy>=2.0.36
alembic>=1.13
psycopg[binary]>=3.2

# External data sourcing
httpx>=0.27                      # TMDB API client

# Auth (email + password): password hashing + signed session cookies.
# Using bcrypt directly (passlib 1.7.4 is unmaintained and breaks on py3.14).
bcrypt>=4.1
itsdangerous>=2.2                # required by Starlette SessionMiddleware

# Dev / tests
pytest>=8.0
